{
  "name": "archforge",
  "private": true,
  "version": "0.1.0",
  "description": "Turn a plain-English prompt into an accurate, explainable system-architecture diagram.",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "cd client && npm install && npx vite build && cd .. && rm -rf dist && cp -r client/dist dist",
    "test": "npm run test --workspace server"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "elkjs": "^0.12.0",
    "express": "^4.21.2",
    "jsonrepair": "^3.15.0"
  },
  "license": "MIT"
}
