{
  "name": "archforge",
  "private": true,
  "version": "0.1.0",
  "description": "Turn a plain-English prompt into an accurate, explainable system-architecture diagram.",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client && rm -rf dist && cp -r client/dist dist",
    "test": "npm run test --workspace server"
  },
  "license": "MIT"
}
